import { Text, View } from "react-native";
import { memoFC, tw } from "@/utils";
import { ScreenContainer } from "@/components";

export const HomeScreen = memoFC(function Screen({}) {
  return <Text>1234</Text>;
});
